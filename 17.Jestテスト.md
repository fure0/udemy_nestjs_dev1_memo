### example.spec.ts

```typescript
describe('Jestの練習', () => {
  it('1+1は2である', () => {
    const result = 1 + 1;
    const expected = 2;
    expect(result).toEqual(expected);
  });
});
```
- ファイル名にspecを入れること

### 実行
```bash
npm run test
```